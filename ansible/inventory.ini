# Replace with the public IPv4 reported by Terraform. The default Ubuntu image
# typically uses the `ubuntu` account; change ansible_user for another image.
[docker_hosts]
your.vm.public.ip ansible_user=ubuntu
