# Replace with the public IPv4 shown in OVHcloud Manager. The default Ubuntu
# image typically uses the `ubuntu` account; change ansible_user for another OS.
[docker_hosts]
your.vm.public.ip ansible_user=ubuntu
