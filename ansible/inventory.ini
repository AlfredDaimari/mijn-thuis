# Replace with the public IPv4 shown in OVHcloud Manager. The default Ubuntu
# image typically uses the `ubuntu` account; change ansible_user for another OS.
# Do not add ansible_password or a private-key path here. run-playbook.sh uses
# the key already loaded into your SSH agent with `ssh-add`.
[docker_hosts]
 146.59.199.134 ansible_user=ubuntu
